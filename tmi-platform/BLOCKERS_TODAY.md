# BLOCKERS TODAY

| Wave | Error/Question | Action Needed | Resolution | Proof |
|---|---|---|---|---|
| Wave 4 | Admin layout source conflict between prior guidance and Gemini note | Verify against FINAL_MERGED_REPO_MANIFEST.md + INSTALL_MATRIX_V2.md before placing apps/web/src/app/contest/admin/layout.tsx | Pending | N/A |
| Wave 0B | Winner reference files unavailable in staging | Hydrate `_imports/tmi-last-2` and `_imports/tmi-last-3` with source drops, then re-run winner preservation copy | Resolved | `_imports/_winning_versions/WinnerRevealPanel.tsx` and `_imports/_winning_versions/SponsorProgressCard.tsx` present |
| Wave 1 | Docs/scripts cannot be copied from staging because staged drops are missing | Hydrate `_imports` with all staged drop folders, then execute Wave 1 copy-only pass | Resolved | Files now present under `docs/contest/control`, `docs/contest/handoffs`, `docs/contest/ops`, and `scripts/PHASE18_*.ps1` |
| Wave Status | Corrected phase status alignment | Keep Wave 2 closed until hydrated-source Wave 0B + Wave 1 evidence is recorded | Resolved | Wave 0A complete; Wave 0B resolved after hydration; Wave 1 resolved after hydrated copy-only pass |
| Wave 2 | Live component placement | Execute approved split-first placement set only, no pages/config/API/Prisma/tests | Resolved (Wave 2 pass complete) | 14 approved component files copied from hydrated `_imports` sources |
| Wave 2 | Do not use tmi-last-6 unless source-of-truth docs explicitly map a file from it | Ignore as out-of-scope source drop for now | Pending/Rule Active | N/A |
| Wave 2 | Bundled-only component families from tmi-last-1 remain unresolved and were intentionally not extracted | Revisit only after safe split-file pass and web build proof | Pending | N/A |
| Wave 2 | Conditional sponsor files from tmi-last-1 may be bundled-adjacent risk | Treat as low-confidence source unless standalone files are present in staged drop | Resolved (standalone confirmed) | `_imports/tmi-last-1/SponsorContestPanel.tsx` and `_imports/tmi-last-1/SponsorROIAnalytics.tsx` copied |
| Wave 2 | Web build proof after safe placement | Run `pnpm -C apps/web build` and capture first blocker/fix | Resolved | Build passed after adding `lucide-react` in `apps/web/package.json` |
| Wave 4 | Locked middleware.ts matcher may not cover /contest/admin routes | Proceed with layout-level admin guard first; verify actual redirect behavior after placement/build | Pending | N/A |
| Wave 4 / Runtime | /contest/admin and /contest/admin/reveal returned 200 during unauthenticated smoke instead of redirect/deny | Investigate layout-level admin guard and session check before final admin onboarding signoff | Active | smoke runtime returned 200 |
| Wave 4 | Same-name destination conflict at apps/web/src/app/contest/page.tsx | Skip overwrite and report conflict; continue placing non-conflicting Wave 4 files | Pending user decision | Existing destination preserved; no overwrite |
| Wave 4 | Source mapping ambiguity for ContestPages/admin-reveal references | Use available staged sources and log mismatch: ContestPages found in tmi-last-1, admin-reveal-page found in tmi-last-2 | Pending source-of-truth confirmation | Placement completed from available staged files |
| Layer 1 | apps/web build failed due to missing Next binary | Repaired by forcing next@14.2.3 in apps/web and rebuilding successfully | Resolved | build exit 0 |
| Build | `next build` intermittently failed during init and later surfaced `Cannot find module ...apps/web/node_modules/next/dist/bin/next` | Repaired install with `pnpm -C apps/web add next@14.2.3 --force` and re-ran build proof | Resolved | `pnpm -C apps/web build` reached “Compiled successfully”, “Generating static pages (53/53)”, “Finalizing page optimization”, and `__EXIT__:0` |
| Layer 3 auth | /contest/admin/contestants and /contest/admin/sponsors remained accessible to unauthenticated users | Verify whether these routes are actually nested under contest/admin layout; patch route-level redirect if needed | Active | Playwright redirect assertions failed |
| Layer 3 auth | Missing guarded child routes for /contest/admin/contestants and /contest/admin/sponsors allowed unauthenticated access | Created explicit deny-by-default route pages under contest/admin and reran build + smoke | Active -> Retest | Pending |
| Layer 4 runtime/env | API boot failed because DATABASE_URL was missing from process env | Loaded DATABASE_URL from apps/api/.env into process session before start:dev and cleared port 4000 collision | Resolved | `GET /api/healthz` = 200, `GET /api/readyz` = 200, and Playwright no longer reports `ECONNREFUSED ::1:4000` |
| Layer 5 UI drift | Contest rules page already had real `h2`; failure source was render-timing flake (`h2` count 0 immediately, >0 after settle) | Patched `tests/e2e/contest.smoke.spec.ts` to wait for first `h2` before counting, preserving semantic requirement; runtime stayed green | Resolved | Before: 17/18. After: smoke rerun passed (18/18) and `test-results/.last-run.json` => `status: passed`, `failedTests: []` |
| Layer 7/8 deploy truth | Pre-deploy env/build truth reconciliation | Collected on 2026-03-15. Branch: main, commit: 9f79846. CI: manual-trigger only (workflow_dispatch). pnpm ci.yml=v9 vs local=10.30.2 (mismatch). 19 tracked modified files uncommitted. Next.js output=standalone (Node required). JWT_SECRET=insecure placeholder. All env values are localhost. Cloudflare: zero config in repo. Hostinger: zero config in repo. | NO-GO on all four platforms | See Layer 7/8 deploy matrix below |

| Layer 7/8 CI/repo truth | pnpm version mismatch in ci.yml (v9) vs lockfile (pnpm@10.30.2) | Patched .github/workflows/ci.yml line 30: version 9 -> 10.30.2 | Resolved (local patch only — not yet committed) | ci.yml now reads version: 10.30.2; lockfile and packageManager field agree |
| Layer 7/8 tracked-file manifest | 20 tracked modified files uncommitted at HEAD 9f79846 | See commit plan below; all 20 are intended finish-mode changes | Pending commit | git diff --stat confirms 2651 insertions, 2093 deletions across app code, schema, lockfile, workspace config |

| Layer 7/8 known-good commit | Staged 107 files; excluded apps/web/package-lock.json (npm accident in pnpm workspace), all ../ parent-dir files, .tmp-*, _imports/, promo-codes-*.json | Surgical git add pass; no noise in staged set confirmed by git diff --cached --name-only with zero ../ matches | Resolved | Commit 7e24a0d: 'chore: known-good checkpoint - contest smoke 18/18, CI pnpm fix, contest module, schema, scripts'; 107 files, 7977 insertions, 502 deletions; deploy remains NO-GO until prod env/hosting resolved |

| Layer 8 prod-env inventory | Production env truth collected 2026-03-15. Checkpoint: main@7e24a0d. Excluded: apps/web/package-lock.json (npm accident), all ../ parent-dir files, .tmp-*, _imports/. Remaining env blockers: JWT_SECRET placeholder; DATABASE_URL localhost; CORS_ORIGINS localhost; ROUTING_STATE_SECRET dev placeholder; all S3/Redis localhost. readyz=503 (DB check failing - Prisma query error on local postgres). healthz=200. Frontend readyz=200. | See prod-env classification sheet in session output | NO-GO | Prod DB/auth/CORS/storage/routing secret all unset; Cloudflare standalone incompatibility unresolved; Hostinger target type unknown |

| Layer 8 hygiene pass | Removed accidental npm lockfile and local session noise artifacts (.tmp*, _imports, promo-codes-*.json, tmi-change-list.txt) | Docs/noise-only cleanup; no app behavior changes | Resolved | Commit 36c1278 removed apps/web/package-lock.json; f5ee7e2 remains last docs/env checkpoint; deploy status unchanged NO-GO |

| Layer 9 hosting/secret truth | Checkpoint lineage confirmed: 36c1278 (hygiene lockfile cleanup), ddef569 (hygiene/docs log). No app behavior/env/deploy-config edits in hygiene phase. Current web mode remains Next standalone (Node runtime required). | Hosting compatibility + secret rotation mapping completed (decision sheet in chat output) | NO-GO | Remaining blockers only: (1) hosting target truth (Hostinger type unresolved; Cloudflare Pages incompatible with standalone) (2) production secret/env rotation plan (DATABASE_URL, API_BASE_URL, NEXT_PUBLIC_API_URL, NEXT_PUBLIC_WS_URL, ROUTING_STATE_SECRET or NEXTAUTH_SECRET, CORS_ORIGINS, NODE_ENV). |

| Layer 10 hosting target truth | Local runbook confirms deploy shape: Frontend target is Hostinger Node.js hosting or Vercel (apps/web), API target is Render (apps/api), Cloudflare role is DNS/CDN/edge (not Pages static host in current path). Source: docs/contest/ops/DEPLOYMENT_RUNBOOK.md. | Hosting compatibility ruling tightened from local docs only; standalone requires Node runtime and matches Hostinger Node/Vercel path. | NO-GO | Remaining blocker now singular: production secret/env rotation and dashboard confirmation (DATABASE_URL, API_BASE_URL, NEXT_PUBLIC_API_URL, NEXT_PUBLIC_WS_URL, ROUTING_STATE_SECRET or NEXTAUTH_SECRET, CORS_ORIGINS, NODE_ENV). |
