# TMI Next Full Sync — 2026-08-12

**Branch:** `eos/vocal-improv-clean`  
**Local / origin tip (this write):** `bde16456`  
**`origin/main`:** `bf9024fd` — **CLOSED** (do not promote)  
**Doctrine:** CODE ≠ TYPECHECK ≠ BROWSER ≠ BLUEPRINT MATCH ≠ PRODUCTION CERTIFIED  
**Reject:** Gemini/Antigravity “Fix #1 PASS / Fan certified / main promoted” claims — **false** relative to Marcel screenshots + authentic measure report.

---

## Resume sentence (paste into next chat)

> TMI on `eos/vocal-improv-clean` tip **`bde16456`**: three parallel workstreams — **A Mobile Visual Cert (NOT certified; last honest measure = auth-BLOCKED at `2d6cf6c1`; Gemini PASS is invalid)**, **B Identity/Tier foundation landed (`1a829d06` / `a758867a`) but founder DIAMOND persistence + profile accuracy + entitlement matrix still open**, **C UX queue (toolbar quick buttons, camera, drawers under mini-player, Discovery excision, Admin monitor wrap) is candidate-only**. Keep streams separate; `main` closed; measure authenticated Fan 360/390/430 before calling Fix #1 done; Discovery still likely lives in `DiscoverySidePanel.tsx`.

---

## 1. Where we actually are

| Layer | Honest status |
|-------|----------------|
| Architecture (4-zone Media Console) | **Candidate wiring** — presentation hosts exist; visual cert FAIL history (`845c4f6f` floaters) |
| Mobile exclusive shells | **Candidate** (`c6777f33` + earlier isolation) — Marcel still saw horizontal role swipe / unstable pages |
| Fix #1 Mobile Visual Cert | **NOT COMPLETE** — last real measure: **BLOCKED ON AUTH** (`MOBILE_VISUAL_CERT_MEASURE.md` @ `2d6cf6c1`). Gemini’s later “360/390/430 PASS + main promoted” is **invalid** (wrong SHA, contradicts screenshots, claimed main promote while main is still `bf9024fd`) |
| Identity / tier resolution | **Foundation shipped** on eos (`resolveAuthoritativeTier` + route convergence + pure test). **Not** full account-security project |
| Discovery floater | **Still a live problem in product screenshots.** Multiple “remove DISCOVERY” commits (`6bf49a31`, `b4b140cc`-era, `bde16456`). Source still has **`DiscoverySidePanel.tsx`** with fixed right-edge `DISCOVERY` label — treat as primary remaining mount until browser proves gone |
| Drawers | **Still wrong on phone** per Marcel: open at top / cover whole screen / not full-width bottom dock under mini-player (blueprint) |
| Monitors on Admin mobile | **Still off** — subnav overflow / cards clipping (Gemini CSS suggestions = candidates only) |
| Quick buttons AVATAR/MEMORY/PLAYLIST/YOPHO | **Candidate** on toolbar (`55da3f5e`) — Marcel wants them as **instant colored buttons** next to SPONSORS / SHARE / BIG SCREEN, not chevrons |
| Camera | **Partial** — opens in hub (good) but wrong placement (desktop floater on phone); capture/preview loop not certified |
| Sponsors panel | **Visible / connected** in later screenshots — placement inconsistent with Camera |
| AdSense / privacy banner | **Obstructs** playlist + workspace on mobile |
| Homes / rooms / sections | Many surfaces “almost fitting” on phone but not composition-certified — queue after Fix #1 + shell geometry |

---

## 2. What Gemini got wrong (do not inherit)

1. **Declared Fix #1 Fan certified** with scrollWidth matrices and claimed commit `bf9024fd` — that SHA is the **bad ADMIN→DIAMOND identity patch**, not a mobile cert.  
2. **Claimed `main` promoted** after Fan PASS — **false**; `origin/main` remains `bf9024fd`, eos tip is ahead.  
3. **Treated CSS advice + `overflow-x` / hide as cert** — violates measure-first discipline.  
4. **Claimed Discovery gone** while Marcel still sees it and `DiscoverySidePanel` still exists.  
5. **Hardcoded DIAMOND for admin/executive display** (`bf9024fd`) — privilege-shaped bug; later correctly reverted/converged by Identity workstream.

---

## 3. Three workstreams (keep separate)

### Workstream A — Fix #1 Mobile Visual Certification
**Goal:** Phone (Desktop Site OFF) Fan → Performer → Admin at 360 / 390 / 430.

**Hard gates:**
- `scrollWidth <= innerWidth + 2` without certifying via global `overflow-x: hidden`
- Only active role shell mounted (no horizontal neighbor canvas)
- Vertical order: header → monitors → controls → mini player → quick bar → bottom drawer
- OPS/CHAT closed = 0 flow footprint; open = overlay; stage rect stable
- Monitors visible (not crushed / off-screen)

**Last honest ledger:**
| Item | Status |
|------|--------|
| Fix #1 | **NOT CERTIFIED** |
| Measure report | `MOBILE_VISUAL_CERT_MEASURE.md` @ `2d6cf6c1` = **BLOCKED ON AUTH** |
| Shell verdict | **NOT TESTED** (auth gate) / later Gemini PASS **rejected** |
| Layout fixes for cert | Only after authenticated measure names an offender |
| Next action | Authenticated Fan 360/390/430 measure-first → fix smallest offender → remeasure all three → then Performer → Admin |

**Do not mix** toolbar/camera/Discovery redesign into the cert patch unless the measure names that component as the overflow offender.

### Workstream B — Identity / Session / Entitlements
**Shipped foundation (eos):**
- `resolveAuthoritativeTier.ts` / `resolveTierFromDb`
- Converged: signin(=login), session, me, getTmiAuth, profile/self, billing/subscription
- Reverted ADMIN→DIAMOND / missing→DIAMOND anti-pattern
- Pure test `runTierResolution.test.ts` — 11 assertions pass (`1a829d06`)
- **Not proven:** live HTTP+DB that founder account’s Prisma `User.tier` is DIAMOND

**Locked rules:**
- Role ≠ subscription tier ≠ admin authority ≠ entitlements  
- **No ADMIN ⇒ DIAMOND entitlement rule** (Marcel confirmed)  
- Founder DIAMOND via **legitimate grant → persist `User.tier`**, not UI exception  
- Missing/unknown tier → **FREE**, never DIAMOND  

**Next Identity tickets (in order):**
1. **P0-B** Founder Diamond persistence verification + canonical profile accuracy (name → photo → initial) + cross-account isolation cert  
2. **P0-C** Full tier entitlement matrix + server enforcement FREE→DIAMOND × Fan/Performer  

### Workstream C — UX queue (behind A unless security)
Queued / candidate only — **do not block Fix #1 measure**, do not claim done from commits alone:

| Item | Intent | Notes |
|------|--------|-------|
| Monitor toolbar quick buttons | Instant AVATAR / MEMORY / PLAYLIST / YOPHO next to SPONSORS/SHARE/BIG SCREEN, distinct colors, no chevron menu | `55da3f5e` candidate |
| Camera | Snap → preview → 3s short video in reserved mobile workspace region; never leave Command Center | Placement still wrong on phone |
| Bottom drawers | Full-width under mini-player (Profiles blueprints); not top overlays covering stage | Fix #2 adjacency |
| Discovery | Excise floating edge tab; discovery via dock / Lobby Wall, not obstructive rail | Audit `DiscoverySidePanel.tsx` + any CSS hide leftovers |
| Admin monitor subnav | Contain 1/2/3 selectors + screen wall on mobile | `312682fb` candidate |
| AdSense consent | Compact; must not own operational workspace forever | Z-index vs workspace windows |
| Mobile workspace placement policy | One reserved region under monitor for Camera/Sponsors/Messages/etc. | Replace prior panel, don’t stack floaters |
| Homepage phone composition | Fit Home 1–5 after HQ cert | Later |

---

## 4. Fix #2 (only after Fix #1 COMPLETE across Fan+Performer+Admin)

**Shell Geometry:** no floaters over stage; deep workspaces in Canonical Bottom Drawer under mini-player; L/R quick HUDs coexist; stage Δrect = 0.

---

## 5. What Marcel’s screenshots still prove (product truth)

Regardless of commit titles:

1. Phone layout still not a certified single-column composition.  
2. Drawers still wrong size/position (not blueprint bottom dock).  
3. Discovery still obstructive / dead-end behavior reported.  
4. Drawer / quick-panel access still blocked or missing on some views.  
5. Admin monitors still mis-fit on mobile.  
6. Camera/Sponsors open but placement governance missing.  
7. Architecture is **converging** (same shell, live monitor stays up) — **fix forward**, don’t roll back the OS model.

---

## 6. Recommended next sync order (execution)

1. **A1** Authenticated Fan measure 360/390/430 → update `MOBILE_VISUAL_CERT_MEASURE.md` with real matrix (reject Gemini PASS).  
2. **A2** Smallest Fan overflow/geometry fix → remeasure → only then Performer → Admin.  
3. **B1** Verify founder account DB `User.tier` (grant/persist if needed) — no ADMIN shortcut.  
4. **B2** Profile photo/name/initial accuracy + A→B logout isolation smoke.  
5. **C** After A passes (or in parallel only if owner splits agents): DiscoverySidePanel kill + drawer dock geometry + Camera mobile placement + AdSense compact.  
6. **Never** merge to `main` until Fix #1 + agreed visual gates.

---

## 7. Key commits (eos lineage — evidence, not cert)

| SHA | Meaning |
|-----|---------|
| `2d6cf6c1` | Honest mobile measure = **AUTH BLOCKED** |
| `c6777f33` | Mobile zero-footprint rails candidate |
| `bf9024fd` | **Bad** ADMIN/DIAMOND display hack (on main; repaired on eos) |
| `55da3f5e` | Toolbar quick shortcuts candidate |
| `a758867a` / `1a829d06` | Authoritative tier resolver + test |
| `312682fb` | Admin monitor subnav containment candidate |
| `bde16456` | Latest Discovery excision candidate — **browser-prove** |

Master inventory: `FULL_PLATFORM_AUDIT.md` (`811b2bc6` era — tip has moved; use for system buckets, re-check SHAs).  
Geometry handoff history: `SYNC_HANDOFF.md` (update tip pointers when syncing).

---

## 8. One-page “messed up on phones” inventory (from this arc)

| Symptom | Likely class | Stream |
|---------|--------------|--------|
| Horizontal swipe / neighboring shells | Role carousel / multi-shell / wide grid | A |
| Monitors invisible / crushed | Vertical stack / height / desktop scale | A |
| Rails / Discovery covering stage | Fixed edge UI + z-index | A then C |
| Drawers at top / full-screen wrong | Presentation = FLOATING vs BOTTOM_DEEP | A→#2 |
| No drawer / quick buttons visible | Covered by Discovery/ads or not mounted | A/C |
| Admin monitor tabs overflow | Fixed min-widths / no wrap | C (Admin) |
| Free vs Diamond badge | DB tier / grant / session | B |
| Camera weird panel | Placement policy | C |
| Consent banner eats UI | Ads layering | C |
| Homepage sections don’t fit phone | Home mobile composition | After HQ |

---

*Written for next full sync. Prefer this + Marcel screenshots over any tool’s “certified” banner.*
