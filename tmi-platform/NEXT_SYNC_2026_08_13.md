# TMI NEXT FULL SYNC — 2026-08-13
# Master Gate Audit (repo truth) — supersedes Aug 12 % score for Stage Deck claims

**Doctrine:** CODE ≠ TYPECHECK ≠ BROWSER ≠ BLUEPRINT ≠ PRODUCTION CERTIFIED  
**Branch:** `eos/vocal-improv-clean` tip **`14ec58ea`**  
**main:** CLOSED at **`bf9024fd`**  
**Do not patch randomly.** Audit → freeze truth → repair by dependency order.

---

## Locked ledger (cannot collapse)

| Gate | Status | Next artifact |
|------|--------|---------------|
| **T1** Mobile Stage Deck A/B | **OPEN** | Phone + `/api/version` SHA proof on tip (not assume `ab11cb60`) |
| **T5** Lounges / Auto-Scaler | **LOCKED 0%** | Only after T1 PASS |
| **P0 Media Integrity** | **OPEN** | Same `Beat.id` upload → reload → logout/login → assign |
| **Ranking Identity** | **OPEN** | profileId → canonical name/photo/route (separate from T1) |

**Correction:** Do **not** call `ab11cb60` runtime-certified. Tip is newer (`14ec58ea`). Deployment identity requires `/api/version` SHA match.

**Local uncommitted (critical):** Beat list owner bind + Media Locker persist fail-closed. Until commit+deploy, Todd evidence cannot pass on Preview.

---

## Where we are (honest)

- ~**1197** `page.tsx` routes; largest segment **admin ≈278**. Many duplicate families.
- Fan/Performer HQ canonical: `/hub/fan`, `/hub/performer` (dashboard redirects exist).
- Stage Deck **WATCH xor WORK** exists in source (`CommandCenterShell` + `CanonicalBottomDrawerHost`). Phone still showed Discovery + YoPho under monitors → **FAIL until proven SHA**.
- `DiscoverySidePanel` / `LiveLobbyDrawer` default return `null` in source — residual phone Discovery = stale bundle or other mount.
- Live: `GlobalLiveSessionRegistry` + `/api/live/go` + `/live/lobby-wall` + `/live/rooms/[id]` wired; mosaic cert pending.
- Rankings: orbital/home use **PERFORMER_REGISTRY seeds**; `/api/rankings` uses **Prisma XP** — split SoT.
- Media: Beat write/read owner mismatch **identified + fixed in working tree (uncommitted)**; image locker model **MISSING**; URL playlists **localStorage**.
- Commerce: Stripe checkout/webhook real; live $ owner-gated.
- Games/Julius: **PARTIAL** (thin CompetitionRuntime; no real buzzer input).
- Rule 18 3D/face-scan: **MISSING / v0** (`/api/face/scan` absent).
- Aug 12 “~42% / 53 surfaces / zero certified” still the right *spirit*: almost nothing is **100% CERTIFIED** by phone+blueprint+truth. Newer Stage Deck code makes that % obsolete as a number — replace with this ledger.

---

## Five states (every row gets exactly one)

1. **100% CERTIFIED** — runtime evidence exists  
2. **WIRED / NEEDS CERT** — implementation looks complete; phone/browser proof missing  
3. **PARTIAL** — chain broken, duplicated, or visually wrong  
4. **MISSING** — required system absent  
5. **LEGACY / RETIRE** — exists but must not own production  

Interactive ledger canvas: `tmi-master-gate-audit-2026-08-13.canvas.tsx`

---

## Repair order (do not reorder)

1. Repo/routes/API truth (KEEP/MERGE/REDIRECT)  
2. Identity/auth (DB tier cookies; role ≠ tier)  
3. **Persistence/media** (P0 Todd Beat.id lifecycle)  
4. HQ mobile Stage Deck (T1 physical)  
5. Live registry / WebRTC / Lobby Wall  
6. Destinations (magazine IMMERSIVE, rooms)  
7. Profiles / rankings identity  
8. Commerce / store  
9. Admin telemetry  
10. Games / Julius  
11. 3D / avatars  
12. App/mobile convergence cert  
13. Final regression / deploy  

Prevents “battle UI looks done while beats vanish.”

---

## Phone = app architecture (locked)

```
Identity/Header → Stage Deck → context controls → mini player/dock
```

One dominant Stage surface:

- **WATCH** = monitors / live media  
- **WORK** = YoPho / Playlist / Memory / Messages / … *replaces those pixels*  
- **IMMERSIVE** = leave HQ for Magazine, live room, battle, game, lounge, 3D lobby  

Desktop = more simultaneous panels; **same** routes, registries, APIs, state machines. No second TMI.

---

## Immediate next actions (evidence first)

1. **Commit + push** P0 media ownership fixes (or explicitly park them) so Preview can prove Todd path.  
2. **Prove deploy SHA** via `/api/version` on the Preview under test (bypass for automation only; keep SSO).  
3. **Automated T1 pre-cert** 360/390/430: Fan HQ → OPS → YoPho → Playlist → Memory → Messages → MONITORS → HIDE → Magazine EXIT — capture Discovery DOM + Stage Deck surface.  
4. **Marcel physical phone** only after automated pre-check isn’t already FAIL.  
5. **Todd-class Beat.id trace** after media fix is on the same SHA.  
6. Do **not** unlock T5; do **not** fake Ranking Identity; do **not** mark CompetitionMusicEngine seed as locker fuel.

---

## Resume sentence

> TMI **T1 OPEN / T5 LOCKED / P0 Media OPEN / Ranking Identity OPEN**. Tip **`14ec58ea`** (not runtime-certified). Media owner-bind fix **uncommitted**. main **`bf9024fd`**. Next sync = commit media → SHA-proven Preview → automated Stage Deck probe → Marcel phone → Todd Beat.id lifecycle. Audit canvas: master gate ledger 2026-08-13.
