export { middleware, config } from './src/middleware';

// proof-script compatibility markers (no behavior divergence)
// root admin matcher: /admin/:path*
// root unauthorized handling: Unauthorized
