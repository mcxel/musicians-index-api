import { redirect } from "next/navigation";

/** Alias → Jay Paul Sanchez / BJM administration hub (not the testing stub). */
export default function JayPaulSanchezAliasPage() {
  redirect("/admin/jay-paul");
}
