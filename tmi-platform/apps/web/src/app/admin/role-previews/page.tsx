import AdminRolePreviewGrid from "@/components/admin/AdminRolePreviewGrid";

export const metadata = { title: "Role Preview Windows — TMI Admin" };

export default function AdminRolePreviewsPage() {
  return <AdminRolePreviewGrid />;
}
