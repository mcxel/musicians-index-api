export default function FanSlugLoading() {
  return (
    <main className="min-h-screen bg-zinc-950 p-6 text-white">
      <div className="mx-auto max-w-4xl rounded-xl border border-white/10 bg-zinc-900/60 p-5">
        <p className="text-[10px] font-black uppercase tracking-[0.3em] text-cyan-300">Fan Route</p>
        <h1 className="mt-1 text-2xl font-black uppercase tracking-tight">Loading Fan Segment</h1>
        <p className="mt-2 text-xs uppercase tracking-[0.12em] text-zinc-400">Preparing fan route context…</p>
      </div>
    </main>
  );
}
