import LaunchBoardSurface from '@/components/launch/LaunchBoardSurface';

export const metadata = {
  title: 'Launch Board — TMI Internal',
  description: 'Internal soft-launch operations dashboard.',
  robots: 'noindex, nofollow',
};

export default function LaunchBoardPage() {
  return <LaunchBoardSurface />;
}
