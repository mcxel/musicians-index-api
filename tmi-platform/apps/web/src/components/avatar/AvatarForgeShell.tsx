"use client";

import AvatarStudioExperience from "@/components/avatar/AvatarStudioExperience";

/**
 * Canonical Avatar Forge entry — same game-style studio as /avatar/studio.
 * RoleGate FAN lives inside AvatarStudioExperience (Rule 26).
 */
export default function AvatarForgeShell() {
  return <AvatarStudioExperience />;
}
