"use client";

/**
 * VenueToolsPanel — canonical venue / lighting / environment controls surface.
 * Inner content for VENUE TOOLS quick panel (Rule 26 role matrix).
 */

export { default } from "./VenueControlPanel";
export type {
  VenueControlRole,
  VenueControlPanelProps as VenueToolsPanelProps,
} from "./VenueControlPanel";
