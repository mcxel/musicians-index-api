/**
 * Mock Event Bus for analytics, bot triggers, and admin observatory
 */
export const emitSystemEvent = (eventName: string, payload: unknown) => {
  // console.log(`[EventBus] ${eventName}`, payload);
};