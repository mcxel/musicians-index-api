"use client";

/**
 * LEGACY — NavigationRail (TMI-OS left-edge console).
 *
 * Production mounts = 0 (unmounted from root layout 2026-08-28).
 * Fail-closed: never render if accidentally re-imported.
 * Canonical destinations: CommandCenterTopNav + AccountCommandMenu + CompactQuickPanelHost.
 */

export default function NavigationRail(): null {
  return null;
}
