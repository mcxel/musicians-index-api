/**
 * GlobalLiveSessionRegistry — client-safe re-export of the in-memory store.
 * API routes must import persistence helpers from GlobalLiveSessionRegistry.server.ts.
 */

export * from "./globalLiveSessionStore";
