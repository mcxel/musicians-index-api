// This file exists to ensure TypeScript has at least one input in packages/db.
// packages/db primarily hosts Prisma artifacts, but we still run a TS gate here
// to validate tsconfig integrity and dependency resolution.
export {};
