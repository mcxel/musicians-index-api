import type { HudTheme } from "@tmi/hud-core";

export const DefaultHudTheme: HudTheme = {
  colors: {
    background: "#000000",
    foreground: "#ffffff",
    accent: "#00b0ff"
  }
};

export default DefaultHudTheme;
